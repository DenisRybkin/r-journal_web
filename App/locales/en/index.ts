import common from "./common.json";
import notifier from "./notifier.json";
import ui from "./ui.json";
import validation from "./validation.json";

export const resources = {
    common,
    notifier,
    ui,
    validation,
}